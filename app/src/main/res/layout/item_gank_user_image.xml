<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="10dp"
    android:layout_marginRight="10dp"
    android:background="@drawable/item_gank_all_bg"
    android:padding="10dp">

    <TextView
        android:id="@+id/tv_saved_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingBottom="5dp"
        tools:text="2017-02-02 22:22:22"/>

    <TextView
        android:id="@+id/tv_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        tools:text="android"/>

    <ImageView
        android:id="@+id/iv_images"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_below="@id/tv_saved_time"
        android:src="@drawable/img_under_constbuction"/>

    <TextView
        android:id="@+id/tv_title"
        android:ellipsize="end"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/iv_images"
        android:layout_alignTop="@id/iv_images"
        android:layout_toRightOf="@id/iv_images"
        android:gravity="center_vertical"
        android:lines="2"
        android:paddingLeft="10dp"
        android:textColor="@color/text_caption"
        android:textSize="18sp"
        tools:text="iOS 和 Android 开发是否要采用 React Native?"/>

</RelativeLayout>